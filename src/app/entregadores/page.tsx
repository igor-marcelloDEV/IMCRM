'use client';
import { useCallback,useEffect,useState } from 'react'; import { CheckCircle2,LogOut,MapPin,Navigation,Package,Truck } from 'lucide-react'; import { toast } from 'sonner'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { createClient } from '@/lib/supabase/client';
export default function DriverPortal(){const [data,setData]=useState<any>({orders:[],contacts:[]});const [codes,setCodes]=useState<Record<string,string>>({});const load=useCallback(async()=>{const r=await fetch('/api/driver/deliveries',{cache:'no-store'});if(r.status===401)return location.href='/entregadores/login';setData(await r.json())},[]);useEffect(()=>{void load()},[load]);async function action(order_id:string,action:string){const r=await fetch('/api/driver/deliveries',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({order_id,action,code:codes[order_id]})});const d=await r.json();if(!r.ok)return toast.error(d.error);toast.success(action==='start'?'Rota iniciada.':'Entrega confirmada!');void load()}const contacts=new Map(data.contacts?.map((c:any)=>[c.id,c])||[]);return <main className="min-h-screen bg-[#f3f5f7] text-slate-900"><header className="sticky top-0 z-10 bg-slate-950 px-4 py-4 text-white shadow"><div className="mx-auto flex max-w-2xl items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-600"><Truck/></span><div className="flex-1"><b className="block">Olá, {data.driver?.name||'entregador'}</b><span className="text-xs text-white/55">{data.orders?.length||0} entrega(s) na rota</span></div><Button variant="ghost" size="icon" onClick={async()=>{await createClient().auth.signOut();location.href='/entregadores/login'}}><LogOut/></Button></div></header><div className="mx-auto max-w-2xl space-y-4 p-4">{data.orders?.length===0?<div className="rounded-2xl bg-white p-10 text-center shadow-sm"><CheckCircle2 className="mx-auto h-12 w-12 text-emerald-500"/><h2 className="mt-3 font-bold">Nenhuma entrega pendente</h2><p className="text-sm text-slate-500">Novas entregas atribuídas aparecerão aqui.</p></div>:data.orders.map((o:any)=>{const c:any=contacts.get(o.contact_id);return <article key={o.id} className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="flex items-center gap-3 border-b p-4"><Package className="text-blue-600"/><div className="flex-1"><b>Pedido #{o.order_code}</b><p className="text-xs text-slate-500">{c?.name||'Cliente'} · {c?.phone}</p></div><span className="rounded-full bg-blue-50 px-2 py-1 text-xs font-semibold text-blue-700">{o.fulfillment_status==='ready'?'Aguardando retirada':'Em rota'}</span></div><div className="space-y-3 p-4"><div className="flex gap-2 text-sm"><MapPin className="h-5 w-5 shrink-0 text-slate-400"/><span>{c?.delivery_address||'Endereço não informado'}</span></div>{c?.delivery_address&&<a target="_blank" rel="noopener" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(c.delivery_address)}`} className="flex h-11 items-center justify-center gap-2 rounded-xl border font-semibold text-blue-700"><Navigation className="h-4 w-4"/>Abrir rota no mapa</a>}{o.fulfillment_status==='ready'?<Button className="h-12 w-full bg-blue-600 text-base font-bold" onClick={()=>action(o.id,'start')}>Iniciar entrega</Button>:<div className="space-y-2"><label className="text-sm font-semibold">Código informado pelo cliente</label><div className="flex gap-2"><Input inputMode="numeric" maxLength={4} value={codes[o.id]||''} onChange={e=>setCodes({...codes,[o.id]:e.target.value.replace(/\D/g,'')})} placeholder="0000" className="h-12 text-center text-xl tracking-[.35em]"/><Button className="h-12 bg-emerald-600 font-bold" disabled={(codes[o.id]||'').length!==4} onClick={()=>action(o.id,'complete')}>Confirmar</Button></div></div>}</div></article>})}</div></main>}
