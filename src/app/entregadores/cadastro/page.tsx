'use client';
import { Suspense,useState } from 'react'; import { useSearchParams,useRouter } from 'next/navigation'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input';
export default function Page(){return <Suspense><Register/></Suspense>} function Register(){const token=useSearchParams().get('token')||'';const router=useRouter();const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm)return setError('As senhas não coincidem.');const r=await fetch('/api/driver/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok)return setError(d.error);router.push('/entregadores/login')}return <main className="flex min-h-screen items-center justify-center bg-[#070b12] p-4"><form onSubmit={submit} className="w-full max-w-sm space-y-4 rounded-2xl border border-white/10 bg-[#101722] p-6 text-white"><div><h1 className="text-2xl font-bold">Criar acesso de entregador</h1><p className="mt-1 text-sm text-white/55">Defina sua senha para acessar as entregas.</p></div>{error&&<p className="text-sm text-red-300">{error}</p>}<Input type="password" minLength={8} placeholder="Senha com pelo menos 8 caracteres" value={password} onChange={e=>setPassword(e.target.value)} required className="h-12 bg-white text-slate-900"/><Input type="password" placeholder="Confirmar senha" value={confirm} onChange={e=>setConfirm(e.target.value)} required className="h-12 bg-white text-slate-900"/><Button type="submit" className="h-12 w-full bg-blue-600 font-bold">Ativar meu acesso</Button></form></main>}
